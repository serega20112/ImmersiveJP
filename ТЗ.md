**Clean Architecture + инженерные практики и удобство разработки из `nsi` + адаптация этих практик под текущий проект, включая frontend.**
---

# 29. TODO из ревью

Обработать замечания по итогам ревью. Пункты сгруппированы по слоям и темам.

## 29.1. DTO и импорты

1. Проработать структуру DTO — она должна быть осознанной и понятной.
2. Добавить докстринги ко всем DTO: для каждого класса и его полей, чтобы новому разработчику было сразу ясно, что это за фигня. Докстринги — на русском.
3. Добавить `__all__` в `__init__.py` пакета `learning` и во все остальные обычные `__init__.py`, где их нет.
4. Убрать глубокие импорты: `from src.application.dto.skill import SkillAssessmentDTO` → `from src.application.dto import SkillAssessmentDTO`. Импорты везде должны быть неглубокими.

## 29.2. Интерфейсы, стиль, общение с пользователем

5. Во всех интерфейсах вместо `...` (Ellipsis) писать `pass` — так нагляднее.
6. Докстринги по всему проекту привести к русскому языку — сейчас получается смесь EN/RU, это некрасиво.
7. Общаться с пользователем на «Вы» — уважать пользователя.

## 29.3. Use cases (learning, profile, mentor)

8. Use cases `learning` разбить архитектурно по папкам, чтобы не смешивать кучу юзкейсов в одной папке.
9. Use case `profile/build_learning_plan` — сейчас заглушка, сделать адекватным: генерация через нейронку.
10. Отправка сообщения ментору: вместо маппинга отдавать запрос пользователя напрямую в нейронку и возвращать ответ нейросети напрямую — чтобы пользователь чувствовал, что общается с настоящим помощником, а не получает 4 захардкоженных значения.

## 29.4. Trust score и key_terms

11. Trust score — продумать, как лучше его сделать. Сейчас это «дичь», но при адекватной проработке имеет право на существование.
12. Key terms захардкожены — убрать. Нужно передавать нейронке тему, на которую надо сгенерить карточку, и запрашивать в заданном формате. А вот формат карточек — уже нам маппить, и это точно делается не в апликационном слое.

## 29.5. Домен и RAG

13. В домене нет сервисов — это не проблема: в RAG это скорее инфраструктурный слой, не нужно выносить отдельными сервисами.
14. RAG не работает — это буквально сервисы нормализации. Необходимо сделать, чтобы RAG реально работал, а не просто прогонял нормализацию.

## 29.6. Инфраструктура

15. Модели в инфраструктуре: добавить докстринги ко всем моделям.
16. `database.py` переименовать в `base.py` в моделях (принятая практика).
17. DI-сервисы разделить — сейчас выглядят некрасиво, «каша».
18. Нормализация (806 строк) — разбить на более мелкие модули.
19. Все в `external/llm` раскидать по папкам в соответствующие файлы, чтобы не было файлов ~1 тыс. строк.

## 29.7. External клиенты и конфигурация

20. Базовый URL нигде не захардкоживать — тянуть из конфига.
22. Kokoro TTS-клиент тянет `api_key`, `base_url`, `voice` напрямую из `os.getenv` — тянуть из сеттингов/конфига (`src/config`).
23. PDF-билдер ~800 строк — раскидать на модули.
24. OpenRouter/Qwen-клиент: захардкоженный `base_url` + `os.getenv` для ключей. В идеале сделать двух LLM-клиентов: текстовый и голосовой.
25. STT и TTS-клиенты — те же замечания, что и к остальным external-клиентам.
26. `save()` в репозитории — докстринг «Обновить существующего пользователя целиком» не соответствует назначению: «save» переводится как «сохранить». Привести семантику и докстринг в соответствие.

## 29.8. Презентация

27. Константы в веб-слое — убрать совсем, тянуть их из сеттингов.
28. Сделать роуты по примеру `auth` и `learning`: без бизнес-логики. Убрать из роутов обработку ошибок — это плохо.
29. Веб-слой: мидлвэйри вынести в слой presentation, раскидав по файлам.
30. Константы в веб-слое убрать — тянуть из сеттингов (хотя сомнительно тянуть сеттинги в презентационный слой — надо продумать).
32. `responses` — сомнительная тема, лучше убрать.
33. Редиректы перенести в HTTP-слой.